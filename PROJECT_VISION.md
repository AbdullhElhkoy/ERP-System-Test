# ChemFlow ERP — تصور المشروع الكامل

## 1. المفهوم الأساسي

نظام ERP متكامل لإدارة شركات صناعية كيميائية متعددة المصانع.
منتج قابل للتكرار لكل شركة (Productized) — كل شيء يُبنى من صفحة الأدمن
(بيانات، وليس كود). أي قيمة مثبتة في الكود يجب نقلها إلى جداول إعدادات.

## 2. المبادئ الحاكمة

1. **الديناميكية الكاملة**: المصانع، الإدارات، الاختبارات، أنواع التعبئة،
   الجريد، أسباب الرفض، قواعد المطابقة، الحقول — كلها تُدار من الأدمن.
2. **نقطة أمان**: لا نرفع أي تعديل على GitHub إلا بقرار صريح.
3. **الصلاحيات تلقائية**: حسب المنصب الوظيفي (مصنع/إدارة → نطاق مشاهدة/تعديل).
4. **التدرج**: كل مرحلة تُختبر وتُثبت قبل الانتقال للتالية.

## 3. الوحدات (Modules)

```
CORE        المصانع، الإدارات، المناصب، المستخدمين، الصلاحيات
EMPLOYEE    الموظفون، الورديات، التعيينات
FACTORY     مراحل التفاعل + المنتج النهائي (أطنان، عينات، جريد، تعبئة)
QUALITY     المعامل، الاختبارات، قواعد المطابقة، الاعتمادات
WAREHOUSING الخامات، المخازن، الأرصدة والحركات
ORDERS      الطلبيات، الحجز، الشحن
REPORTS     التقارير والتحليلات
API         واجهة برمجية للتطبيق المستقبلي
```

## 4. تدفق البيانات الأساسي

```
استلام خام ← فحص ← قرار (قبول/رفض/خصم) ← مخزون
    ↓
تفاعل (مراحل) ← عينات ← نتائج تحاليل
    ↓
منتج نهائي ← أطنان ← عينات ممثلة ← نتائج فيزيائية/كيميائية ← جريد
    ↓
تعبئة (نوع تعبئة) ← تحويلات ← مخزون أرضي
    ↓
تسليم للمخزن ← طلبية ← حجز ← شحن
```

## 5. الوضع الحالي للكود (ماذا يوجد بالفعل)

| الوحدة | الحالة |
|---|---|
| CORE (plants, user_account, custom_permissions) | موجودة ومكتملة بنيوياً |
| EMPLOYEE | موجودة (ShiftType, ShiftGroup, Employee, Assignment) |
| FACTORY (shared/reaction/final_product) | موجودة — بها ثغرات وتكرار |
| QUALITY (shared_definitions) | موجودة (QualityGrade) |
| WAREHOUSING (raw_materials) | موجودة بنيوياً (خامات، مخازن، حركات، عينات) |
| ORDERS | موجودة بنيوياً (طلبيات، توزيع، حركات) |
| REPORTS | **غير موجودة** |
| API | **غير موجودة** (رغم تثبيت rest_framework) |

## 6. الثغرات المعروفة في الكود الحالي

1. ~~**تكرار منطق الحفظ**: `_save_rows` (factory/views.py) و~~
   ~~`_save_final_product_entry` (factory/admin.py) — نفس العمل بمسارين.~~
   ✅ مُعالج: طبقة Service موحّدة (`services.py`).
2. ~~**خطأ scope**: views.py يستخدم `scope="process"` بينما القيمة الصحيحة~~
   ~~`"reaction"` — فلن يظهر أي اختبار تفاعل.~~
   ✅ مُعالج.
3. ~~**حقول لا تُحفظ**: `sampling_status` و `result_time` تُرسل من القالب~~
   ~~ولا يوجد لها حقل في الموديل.~~
   ✅ مُعالج (أُضيف الحقلان في مهاجرة 0012).
4. ~~**sample_code**: حقل موجود في OutputReading ولا يُملأ أبداً.~~
   ✅ مُعالج: توليد تلقائي `S-<ton_code>` في الخدمة.
5. ~~**مخزون أرضي (FloorStock)**: موديلات موجودة بلا منطق تشغيل ولا أدمن.~~
   ✅ مُعالج: إضافة/خصم تلقائي عند إنشاء طن بجريد + أدمن.
6. ~~**QualityConformityResult**: لا يُنشأ أبداً رغم تعريف قواعد المطابقة.~~
   ✅ مُعالج: تطبيق تلقائي من قواعد المطابقة حسب تصنيف الجريد.
7. ~~**PackingEvent/Conversion**: معرّفان لكن غير مدمجين في شاشة الإدخال.~~
   ✅ مُعالج: إنشاء حدث تعبئة تلقائي من كمية التعبئة المدخلة.
8. ~~**الحقول الديناميكية (FieldDefinition)**: إدارة فقط، غير مدمجة في الإدخال.~~
   ✅ مُعالج: مدمجة في شاشة الإدخال وتُحفظ في `dynamic_data`.
9. **لا اختبارات** (tests.py فارغ).

## 7. خطة العمل

### المرحلة 0 — تثبيت الأساس
- توحيد منطق الحفظ في طبقة Service واحدة.
- إصلاح خطأ scope.
- إضافة حقول sampling_status و result_time وحفظها.
- توليد sample_code تلقائياً.

### المرحلة 1 — استكمال دورة المنتج
- تفعيل FloorStock (إضافة تلقائية عند إنشاء طن بجريد، خصم عند التسليم).
- تطبيق QualityConformityResult تلقائياً من قواعد المطابقة.
- دمج PackingEvent في شاشة الإدخال.
- تفعيل الحقول الديناميكية في شاشة الإدخال.

### إعادة هيكلة الـ Factory Context
- صفحة Factory: قائمة المصانع فقط (دخول المصنع الوحيد).
- لوحة المصنع: 4 أقسام (Factory Settings / Data Entry / Reports / Data Analysis).
- Factory Settings مقسمة: Reaction Settings + Final Product Settings + بيانات المصنع.
- Data Entry مقسمة: Reaction + Final Product (المراحل وأنواع التعبئة من إعدادات المصنع الحالي فقط).
- إزالة Factory Selector من كل الصفحات الداخلية — السياق من الجلسة فقط.
- إخفاء الـ 22 موديل من قائمة /admin/ — الوصول فقط من داخل المصنع.

### المرحلة 2 — التقارير والتحليلات
- لوحة قيادة عامة (إنتاج، جودة، أرصدة، اتجاهات تحاليل).
- تصدير Excel.

### المرحلة 3 — طبقة API
- Django REST Framework + JWT.
- Endpoints لكل العمليات والاستعلامات.
- تطبيق الصلاحيات حسب المنصب.

### المرحلة 4 — تطبيق Flutter
- موبايل + ويب من كود واحد.
- شاشات حسب الدور (إدخال، معمل، مدير).

### المرحلة 5 — المبيعات والأوامر
- ربط orders بالإنتاج والمخزن حسب الجريد الفعلي.

## 8. نقاط رجوع آمنة

- Tag: `v1.0-baseline` على GitHub (نسخة ما قبل التعديلات).
- الرجوع: `git checkout v1.0-baseline`
